# NEXT STEPS

## 🔥 Approved

- Create a GitHub repository for the website.
- Add the GitHub remote to this local folder.
- Commit and push the staged public site files.
- In Netlify, import the GitHub repository.
- Use no build command.
- Set publish directory to `.`.
- Add the custom domain and enable HTTPS.
- Compress the largest images in `assets/` so the site loads faster on phones.
- Check every page on mobile, especially image-heavy work pages.
- Add Open Graph preview metadata so links look good when shared.
- Add a favicon and browser tab title polish.

## 💡 Ideas

- Add a persistent audio object that follows the visitor through the website without turning the site into a normal clean portfolio.
- Explore a small fixed MP3 player in the corner with play/pause, track title, and volume.
- Explore a hidden radio/transmission layer that starts only when clicked, then keeps looping quietly.
- Explore different ambient loops per section: car interior, corrupted menu music, voice memo fragments, machine hum, breath, broken UI sounds.
- Make the audio player look like a damaged device, not a generic Spotify widget.
- Use a simple playlist with local `.mp3` files in `assets/audio/`.
- First audio version could add `assets/audio/`, place one short loop or track inside it, add a fixed bottom-left control, save play state/volume in `localStorage`, and show the same player on work pages.
- Decide whether the site should keep the current raw HTML structure or move into a small static-site system later.
- Try a Whisperer gradient treatment.
- Open down work when hovering text. Classification unsure: kept as an idea because the wording needs confirmation.
- Sound videos: random Whisperer example.

## 🧪 Bugs

- None found in existing TODO/FIXME/code notes during backfill.

## ⛔ Blocked

- Audio direction is blocked on content decisions: first sound source/feeling, whether sound is atmosphere/artwork/navigation, and whether visitors choose tracks or the site behaves like one haunted broadcast.

## ✅ Done

- Added A Bird That Mistook a Street Lamp for the Sun as a work page with PDF-sourced installation/game images and itch link/embed.
- Static site structure exists with raw HTML/CSS/JS pages for the archive, work pages, music, and external log.
- Latest git commit prepared the static site for publishing.
